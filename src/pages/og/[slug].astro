---
import type { GetStaticPaths } from 'astro'

export const getStaticPaths: GetStaticPaths = async () => {
  let posts = await Astro.glob('../blog/**.md')
  return posts.map((post) => ({
    params: { slug: post.url?.split('/').at(-1) },
    props: { title: post.frontmatter.title }
  }))
}

let { title } = Astro.props
---

<div
  class="flex flex-col items-center justify-center w-[1200px] relative mx-auto h-screen bg-slate-50"
>
  <!-- <div
    class="absolute top-0 w-screen h-[10px] bg-gradient-to-r from-[rgb(30,64,175)] to-[rgb(96,165,250)]"
  >
  </div>
  <div class="mt-10 text-6xl font-bold leading-[1.3] text-center">
    {title}
  </div>
  <div class="flex items-center gap-10">
    
    <div class="text-5xl font-light">
      <span>Hi, I'm&nbsp;</span>
      <span
        class="block tracking-wide text-transparent md:inline theme-gradient bg-clip-text whitespace-nowrap"
      >
        Austin Crim
      </span>
      <div>
        <div class="mt-2">and I wrote something for you.</div>
      </div>
    </div>
  </div> -->
  <div class="gradient-container h-3/4 rounded-xl p-1 shadow-md m-8">
    <div
      class="flex flex-col space-y-20 h-full justify-center heading p-10 bg-white bg-opacity-60 text-slate-600"
    >
      <h1 class="inline text-7xl font-bold leading-[1.3]">
        {title}
      </h1>
      <div
        class="flex items-center justify-between max-w-4xl mx-auto space-x-12"
      >
        <img
          src="https://pbs.twimg.com/profile_images/1277774477099773954/cNUzQvNX_400x400.jpg"
          class="object-cover w-32 border-2 border-blue-300 h-32 rounded-full"
        />
        <p class="text-4xl">I'm Austin and I wrote something for you.</p>
      </div>
    </div>
  </div>
</div>
